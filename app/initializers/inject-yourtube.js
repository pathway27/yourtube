import { inject as service } from '@ember/service';

export function initialize(application) {
  // application.inject('route', 'yourtube', 'service:yourtube-api');
  // console.debug('Yourtube Service injected to every route!');
}

export default {
  initialize
};
